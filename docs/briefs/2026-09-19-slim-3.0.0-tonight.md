# Slim redesign → 3.0.0 tonight: visual-first scope and process (2026-09-19)

Kamil wants **3.0.0 in testers' hands tonight**: the new graphics now, everything else polished next week as 3.0.1. The binding brief is `docs/briefs/2026-09-19-slim-redesign-brief.md` (Polish; this branch only). **This file narrows that brief for tonight and overrides it where they differ.**

## Base and branches

- Base: `origin/main` at `43cc2201` (phase 0 on main: shared primitives, ADR-209 in `docs/Decisions.md`). Read ADR-209 first and USE its primitives: `YoBadge` live variant, `AvailabilityDot`, `YoWaveform`/`StoryWaveform`, `HomeSectionHeader`, the `MomentStoryTile` ring, `VoicePlayerRow`, `YoChannelRow`/`YoVoiceChannelRow`, `YoMetricPill`, `YoServerRailItem`/`YoServerTile`.
- One branch per phase: `slim/pN` created from `43cc2201`. Push the branch to `origin` (branches do not trigger CI or any deploy). When the phase is done and green, push the tag `slim-pN-ready` on its final commit. Never push `main`; the Mac session integrates all seven branches, runs the full suite, reviews, sets `3.0.0+34` and pushes `main`.

| Phase | Where | Screens |
| --- | --- | --- |
| p1 Start | Mac | Home phone + desktop |
| p2 Serwery | Mac | Servers directory + server workspace |
| p3 Czaty | PC | `messages_screen.dart`, `chat_screen.dart` |
| p4 Momenty | PC | YO Moments chrome, Głos and Yeels feeds, moment detail |
| p5 Profil | PC | `profile_screen.dart`, `friend_profile_screen.dart`, `profile_header.dart` |
| p6 Więcej | PC | More sheet, Settings, Friends (list, invitations, search), Notifications |
| p7 Logowanie | PC | `responsive_auth_screen.dart` chain (login/register phone+desktop, forgot password, verify email, TOTP body background, check-inbox sheet) |

## Hard rules (unchanged, non-negotiable)

- Presentation only. Never remove functionality, a screen, an action, a gesture or a code path. No Firestore schema, rules or Functions changes. No fake data, users, counts or presence.
- Never edit a test assertion, finder or rhythm number to go green. Fix the code.
- No new inline hex colours: `AppColors`, `AppPalette`, `AppImmersiveColors` only. Material 3 only.
- Do not touch `pubspec.yaml`, `lib/features/home/presentation/widgets/navigation/yo_floating_navigation_dock.dart` or the navigation model in `main_shell.dart`.
- Głos and Yeels stay two separate formats with their existing filters (`MomentsFilter` for Głos, `ReelFeedScope` for Yeels) and keys.
- The YO Voice logo mark stays visible on Start.
- Legal hygiene: the names Instagram / Discord / Twitch never appear in UI, copy or store text.
- Keep every key, semantics label, callback and test contract. Keep Dark and Pearl, narrow/medium/wide, 1.0/2.0 text scale and all states working.

## Shared-file rules (seven branches merge tonight)

- Theme files: only p1 may edit `app_radius.dart` / `app_theme.dart` / `app_spacing.dart`; only p7 may add roles to `app_immersive_colors.dart` (additive).
- `lib/shared/widgets/**`: additive optional parameters only, and only if unavoidable; say so in the commit body.
- Localization catalogs: **no edits tonight**. Use existing keys; if new copy seems necessary, defer the item.
- `docs/`: no edits in phase branches (the integration writes the docs).
- New capture harnesses: `test/slim_<key>_capture.dart` (no `_test` suffix).

## Tonight's scope per phase (MUST = ships in 3.0.0, DEFER = 3.0.1)

- **p1 Start** MUST: logo mark in the greeting row; Moments rail with the stories ring; "Na żywo teraz" cards 16:9 with the live badge and real-data metric pill; servers as a compact list (`YoServerTile`); slim section headers; no data-less hero banners. DEFER: desktop Start layout rework beyond restyling.
- **p2 Serwery** MUST: directory as a compact list; workspace server rail with `YoServerRailItem` on narrow and wide; channel column already on `YoChannelRow`; slim headers; flattened cards. DEFER: fourth "members" column on wide, members sheet restructure.
- **p3 Czaty** MUST: chats list rows 68 px, active-people rail with `AvailabilityDot` / `PeopleStatus`, "WIADOMOŚCI" label, existing unread badge; conversation: slim app bar, bubble and date-separator styling, slim 48 px composer (visual only; keyboard-dismissal and TapRegion/PopScope behaviour from Build 31 unchanged). DEFER: author grouping of consecutive messages if it needs non-trivial list logic.
- **p4 Momenty** MUST: slimmer `YoMomentsHeader`, `YoMomentsFormatSwitch`, `YoMomentsFilterChips`, `YoMomentsLocalPanel`, `ImmersiveFeedChrome`; edge-to-edge media with scrim, right action column, 2 px progress above the dock; all keys and labels unchanged. DEFER: anything that changes playback or feed logic.
- **p5 Profil** MUST: header (avatar, stats row from real fields, one primary + one secondary CTA + icon) on own and friend profile; flatten card-in-card; followers only when `canExposeCreatorAudience`. DEFER: Głos / Yeels / Serwery tabs with the 3-column grid; disabled "Wkrótce" tabs on the friend profile.
- **p6 Więcej** MUST: `HomeSectionHeader`, row styling and flattened cards on More, Settings, Friends and Notifications. DEFER: any restructuring.
- **p7 Logowanie** MUST: auth backgrounds without waves or three-layer gradients (splash untouched), no card-in-card, mode rail as a flat primary pill (outline `authBorderStrong`), solid primary button without glow, fields 52 px with `authBorderStrong` outline and 2 px focus, provider buttons `OutlinedButton` 48 px min, flat desktop brand panel with faint arcs, calmer forgot-password / verify-email / TOTP backgrounds. Role literals `authFocus`, `authLink`, `authBrandPanel` in `AppImmersiveColors` with the equality assertions in `app_theme_test.dart` and registration in `semantic_color_source_guard_test.dart` as the brief says. DEFER: catalog label changes ("Sign in"/"Sign up" keys for 41 locales), the `auth-footer-switch` link, the screenshot-harness extension. Keep today's labels.

## Process per phase

1. Re-read every file before touching it. Implement the MUST list.
2. `flutter analyze` clean. Targeted tests green without assertion changes: the tests the brief lists for the phase plus every test file importing a file you changed.
3. One frame set: 390 and 1440, Dark and Pearl, pl, populated (existing harness or a new `test/slim_<key>_capture.dart`). Look at every frame; fix overflow, clipped text, lost states, wrong theme colours.
4. Conventional commits (`feat(ui): slim <key> (phase N)` …) with a why-body listing MUST done, DEFER items, deviations and where the frames are, ending with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
5. `git push origin slim/pN`, then `git tag slim-pN-ready && git push origin slim-pN-ready`.

Every command must finish in under 8 minutes (split long runs); never leave a dirty tree behind.
